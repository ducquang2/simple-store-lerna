mutation UpdateItemCountFromCart(
  $username: String!
  $itemId: ID!
  $itemCount: Int!
) {
  UpdateItemCountFromCart(
    username: $username
    itemID: $itemId
    itemCount: $itemCount
  ) {
    username
    itemID
    itemCount
  }
}
